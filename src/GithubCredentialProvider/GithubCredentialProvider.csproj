<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>GithubCredentialProvider</RootNamespace>
    <AssemblyName>nuget-plugin-github-credential-provider</AssemblyName>
    <PublishSingleFile>false</PublishSingleFile>
    <SelfContained>false</SelfContained>
    
    <!-- NuGet Package Metadata -->
    <IsPackable>true</IsPackable>
    <PackageId>nuget-plugin-github-credential-provider</PackageId>
    <Authors>Jason Walker</Authors>
    <Product>GitHub NuGet Credential Provider</Product>
    <Description>
      A NuGet cross-platform credential provider plugin that uses the GitHub CLI (gh) to authenticate with GitHub
      Package feeds. Supports GitHub.com and GitHub Enterprise Server
    </Description>
    <PackageTags>nuget-plugin;credential-provider;github</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <RepositoryUrl>https://github.com/ungood/nuget-plugin-github-credential-provider</RepositoryUrl>
    <PackAsTool>true</PackAsTool>
    <PackageType>DotnetTool</PackageType>
    <ToolCommandName>nuget-plugin-github-credential-provider</ToolCommandName>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
  </PropertyGroup>
  
  <ItemGroup>
    <None Include="../../README.md" Pack="true" PackagePath="\"/>
  </ItemGroup>
  <ItemGroup>
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">
      <_Parameter1>GithubCredentialProvider.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.4" />
    <PackageReference Include="NuGet.Protocol" Version="7.0.1" />
  </ItemGroup>
</Project>
